{
  "name": "vue-project",
  "version": "0.0.0",
  "scripts": {
    "db:serve": "json-server --watch data.json --port=3001",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.1",
    "vite": "^3.0.1"
  }
}
